<?xml version="1.0" encoding="utf-8"?>
<ViewAnimator xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:id="@+id/animator"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:inAnimation="@android:anim/fade_in"
              android:outAnimation="@android:anim/fade_out">

    <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/pbLoading"
            android:layout_gravity="center"
            android:indeterminateOnly="true"
            android:theme="@style/CircularProgress"/>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:context=".MainActivity">

        <LinearLayout android:layout_width="match_parent"
                      android:layout_height="0dp"
                      android:orientation="horizontal"
                      android:id="@+id/tvInfo"
                      android:gravity="center_horizontal"
                      app:layout_constraintVertical_weight="1"
                      app:layout_constraintBottom_toTopOf="@id/btnRealm"
                      app:layout_constraintLeft_toLeftOf="parent"
                      app:layout_constraintRight_toRightOf="parent"
                      app:layout_constraintTop_toTopOf="parent">

            <ProgressBar
                    android:id="@+id/pbRealm"
                    style="?android:attr/progressBarStyleHorizontal"
                    android:layout_width="36dp"
                    android:layout_height="match_parent"
                    android:layout_margin="24dp"

                    android:progressDrawable="@drawable/pb_realm"
                    android:indeterminate="false"
                    android:max="100"
                    android:progress="20" />

            <ProgressBar
                    android:id="@+id/pbSql"
                    style="?android:attr/progressBarStyleHorizontal"
                    android:layout_width="36dp"
                    android:layout_margin="24dp"

                    android:layout_height="match_parent"
                    android:progressDrawable="@drawable/pb_sql"
                    android:indeterminate="false"
                    android:max="100"
                    android:progress="20" />

        </LinearLayout>

        <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRealm"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_margin="24dp"
                android:text="@string/start_realm"
                app:layout_constraintHorizontal_weight="1"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toLeftOf="@id/btnSql"
                app:layout_constraintTop_toBottomOf="@id/tvInfo"/>

        <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSql"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_margin="24dp"
                app:layout_constraintHorizontal_weight="1"
                android:text="@string/start_sql"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toRightOf="@id/btnRealm"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tvInfo"/>

        <!--<TextView-->
                <!--android:id="@+id/tvInfo"-->
                <!--android:layout_width="wrap_content"-->
                <!--android:layout_height="wrap_content"-->
                <!--android:text="Hello World!"-->
                <!--app:layout_constraintBottom_toBottomOf="parent"-->
                <!--app:layout_constraintLeft_toLeftOf="parent"-->
                <!--app:layout_constraintRight_toRightOf="parent"-->
                <!--app:layout_constraintTop_toBottomOf="@id/btnRealm"/>-->

    </androidx.constraintlayout.widget.ConstraintLayout>

</ViewAnimator>

